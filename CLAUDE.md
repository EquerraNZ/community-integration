# CLAUDE.md

This repository uses Spec-Driven Development. The canonical contract for every
agent (the SDD workflow, the constitution, BCQuality citation rules, the
mandatory verifier set, and the house rules) lives in `AGENTS.md` at the repo
root. Read it and follow it.

The import below loads `AGENTS.md` into context automatically.

@AGENTS.md
